dist: trusty
sudo: required
language: node_js
node_js:
  - "8"
addons:
  chrome: stable
before_install:
  rm -rf ./node_modules package-lock.json
install: "npm install"
cache:
  directories:
    - node_modules
notifications:
  email: false
